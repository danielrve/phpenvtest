ci_pipeline: 
       on:
        push:
        branches:
          - main
       
        steps:
          - name: Execute script
            env:
              DBNAME: ${{ secrets.DBNAME }}
              LASTNAME: ${{ secrets.DANIEL }}              
       run: php index.php
       
